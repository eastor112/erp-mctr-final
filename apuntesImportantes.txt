Para solucionar el problema al recargar la página

https://www.npmjs.com/package/vite-plugin-rewrite-all

npm install --save-dev vite-plugin-rewrite-all

//poner en la configuracion
import pluginRewriteAll from 'vite-plugin-rewrite-all';

export default {
  plugins: [pluginRewriteAll()]
}